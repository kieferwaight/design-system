/**
 * Standard size keys for system icons.
 * Aligned with other visual elements (such as Swatches and Avatars).
 */
export type IconSize = "xs" | "sm" | "md" | "lg" | "xl";

export interface IconInfo {
  token: string;
  sizePx: number;
}
