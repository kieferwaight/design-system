/**
 * Backdrop blur tiers available in the design system.
 * Use sparingly as active backdrop-filters require significant GPU overhead.
 */
export type BlurTier = "sm" | "md" | "lg" | "xl";

/**
 * Ambient box shadow depths.
 * Designed to represent the physical altitude (z-axis depth) of a surface.
 */
export type ShadowDepth = "sm" | "md" | "lg";
