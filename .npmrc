minimum-release-age=0
# Skip the pre-run install/lockfile check before `pnpm run <script>` — it was what
# broke `pnpm stories` originally. Our scripts call the local binaries directly.
verify-deps-before-run=false
